class bit
{
	public:
		int value;
		
	bit(void)
	{
		value = 0;
	}
	
	/*
	bit bit::adder(bit bit1, bit bit2, int carry)
	{
		int result = bit1.value + bit2.value + carry;
		if (result == 1 || result == 3)
			bitRES.value = 1;
		if (result == 2 || result == 3)
			carry = 1;
	}
	
	for(
		*/
}