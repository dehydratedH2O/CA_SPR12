all ASM code *must* be lowercase.
all ASM code *must* have only one space between the mnemonic and operands, and no spaces elsewhere.
all register references must be of the form $rx where x is the register number
there must be only one comma and no spaces between each operand
there is to be no terminating character and the end of each line -- only a newline
